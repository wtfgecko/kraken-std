apiVersion: v1
kind: Service
metadata:
  name: {{ include "kraken-example.fullname" . }}
  labels:
    {{- include "kraken-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kraken-example.selectorLabels" . | nindent 4 }}
